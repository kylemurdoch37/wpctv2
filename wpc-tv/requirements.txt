# WPC-TV Requirements
# This file lists Python package dependencies for the WPC-TV application
# Currently, the application uses only Python standard library modules

# Uncomment and add dependencies as needed, for example:
# textual>=0.1.0           # For terminal UI
# python-dotenv>=1.0.0     # For environment configuration
# requests>=2.31.0         # For API calls
